# Optional. A pokemontcg.io key (free from dev.pokemontcg.io) used by the
# /api/cards function. Set it in Vercel under Settings > Environment Variables,
# or copy this file to .env.local for `npm run dev`.
POKEMONTCG_API_KEY=
